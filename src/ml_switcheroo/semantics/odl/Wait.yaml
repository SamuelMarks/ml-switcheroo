operation: Wait
variants:
  paxml:
    dispatch_rules: []
    macro_template: '{future}.block_until_ready() if hasattr({future}, ''block_until_ready'')
      else None'
    pack_as: Tuple
    required_imports: []
  flax_nnx:
    dispatch_rules: []
    macro_template: '{future}.block_until_ready() if hasattr({future}, ''block_until_ready'')
      else None'
    pack_as: Tuple
    required_imports: []
