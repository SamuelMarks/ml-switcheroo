operation: Aminmax
variants:
  paxml:
    dispatch_rules: []
    macro_template: (jnp.min({input}, axis={dim}, keepdims={keepdim}), jnp.max({input},
      axis={dim}, keepdims={keepdim}))
    pack_as: Tuple
    required_imports: []
  flax_nnx:
    dispatch_rules: []
    macro_template: (jnp.min({input}, axis={dim}, keepdims={keepdim}), jnp.max({input},
      axis={dim}, keepdims={keepdim}))
    pack_as: Tuple
    required_imports: []
